{
  "name": "parlour-root",
  "private": true,
  "workspaces": ["desktop", "packages/*", "parlour-cli"],
  "scripts": {
    "dev": "bun run --cwd desktop dev",
    "build": "bun run --cwd desktop build",
    "test": "bun run --cwd desktop test",
    "rebuild": "bun run --cwd desktop rebuild",
    "dist": "bun run --cwd desktop dist",
    "dev:browser": "bun run --cwd packages/app dev",
    "storybook": "bun run --cwd packages/ui storybook",
    "test:unit": "bunx vitest run"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/addon-web-links": "^0.12.0",
    "@xterm/addon-webgl": "^0.19.0",
    "@xterm/xterm": "^6.0.0"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
